import { useEffect, useMemo, useState } from 'react'

const demoProducts = [
  { id: 1, name: 'Galaxy S23 Ultra', category: 'Phones', price: 680000, condition: 'Like new', location: 'Kigali', seller: 'Demo seller', image: 'https://images.unsplash.com/photo-1610945265064-0e34e5519bbf?auto=format&fit=crop&w=900&q=80', description: 'A premium smartphone with a bright display, excellent camera system and all-day battery.' },
  { id: 2, name: 'MacBook Air M2', category: 'Laptops', price: 1250000, condition: 'Good', location: 'Kigali', seller: 'Demo seller', image: 'https://images.unsplash.com/photo-1541807084-5c52b6b3adef?auto=format&fit=crop&w=900&q=80', description: 'Lightweight laptop for creative work, study and everyday productivity.' },
  { id: 3, name: 'Sony WH-1000XM5', category: 'Audio', price: 310000, condition: 'New', location: 'Huye', seller: 'Demo seller', image: 'https://images.unsplash.com/photo-1546435770-a3e426bf472b?auto=format&fit=crop&w=900&q=80', description: 'Comfortable wireless headphones with rich sound and noise cancellation.' },
  { id: 4, name: 'Canon EOS M50', category: 'Cameras', price: 590000, condition: 'Good', location: 'Kigali', seller: 'Demo seller', image: 'https://images.unsplash.com/photo-1516035069371-29a1b244cc32?auto=format&fit=crop&w=900&q=80', description: 'A versatile mirrorless camera ready for photos, video and content creation.' },
  { id: 5, name: 'Nintendo Switch OLED', category: 'Gaming', price: 480000, condition: 'Like new', location: 'Musanze', seller: 'Demo seller', image: 'https://images.unsplash.com/photo-1578303512597-81e6cc155b3e?auto=format&fit=crop&w=900&q=80', description: 'A portable and living-room gaming console with a vivid OLED display.' },
  { id: 6, name: 'Samsung 55” Smart TV', category: 'TVs', price: 720000, condition: 'New', location: 'Kigali', seller: 'Demo seller', image: 'https://images.unsplash.com/photo-1593784991095-a205069470b6?auto=format&fit=crop&w=900&q=80', description: 'Bring streaming, sports and films to life with a crisp smart television.' },
  { id: 7, name: 'iPad Air 5th Gen', category: 'Tablets', price: 735000, condition: 'Like new', location: 'Kigali', seller: 'Demo seller', image: 'https://images.unsplash.com/photo-1544244015-0df4b3ffc6b0?auto=format&fit=crop&w=900&q=80', description: 'A powerful, portable tablet for notes, study, design and entertainment.' },
  { id: 8, name: 'JBL Flip 6 Speaker', category: 'Audio', price: 165000, condition: 'New', location: 'Rubavu', seller: 'Demo seller', image: 'https://images.unsplash.com/photo-1608043152269-423dbba4e7e1?auto=format&fit=crop&w=900&q=80', description: 'Portable waterproof sound with a bold bass response and long battery life.' },
  { id: 9, name: 'Apple Watch Series 8', category: 'Accessories', price: 390000, condition: 'Good', location: 'Kigali', seller: 'Demo seller', image: 'https://images.unsplash.com/photo-1546868871-7041f2a55e12?auto=format&fit=crop&w=900&q=80', description: 'A smart everyday companion for activity, notifications and health tracking.' },
  { id: 10, name: 'Dell XPS 13', category: 'Laptops', price: 980000, condition: 'Good', location: 'Huye', seller: 'Demo seller', image: 'https://images.unsplash.com/photo-1496181133206-80ce9b88a853?auto=format&fit=crop&w=900&q=80', description: 'A compact Windows laptop with a clean display and comfortable keyboard.' },
]
const categories = ['All', 'Phones', 'Laptops', 'Tablets', 'TVs', 'Cameras', 'Audio', 'Gaming', 'Accessories']
const money = (value) => `RWF ${value.toLocaleString('en-US')}`
const ui = {
  home: ['Home', 'Ahabanza'], marketplace: ['Marketplace', 'Isoko'], repair: ['Repair desk', 'Ikigo cyo gusana'], categories: ['Categories', 'Ibyiciro'], sell: ['Sell a product', 'Gurisha igikoresho'], account: ['Account', 'Konti'], cart: ['Cart', 'Igitebo'], search: ['Search electronics', 'Shakisha ibikoresho'],
  heroKicker: ["RWANDA'S ELECTRONICS MARKETPLACE", 'ISOKO RYIBIKORESHO BYIKORANABUHANGA MU RWANDA'], heroTitle: ['Good tech.', 'Ikoranabuhanga ryiza.'], heroTitle2: ['Better finds.', 'Hitamo neza.'], heroCopy: ['Discover electronics from sellers across Rwanda, or list your own products and reach more buyers.', 'Shakisha ibikoresho byikoranabuhanga ku bacuruzi bo mu Rwanda, cyangwa ushyire ku isoko ibyawe ubone ababigura.'], shop: ['Shop electronics', 'Gura ibikoresho'], sellShort: ['Sell a product', 'Gurisha igikoresho'], made: ['Made for Rwanda', 'Byakorewe u Rwanda'], simple: ['Simple, local, trustworthy', 'Byoroshye, hafi kandi byizewe'], fresh: ['Fresh from the marketplace', 'Ibikoresho bishya ku isoko'], viewAll: ['View all products', 'Reba ibicuruzwa byose'], shopType: ['What are you looking for?', 'Urashaka iki?'],
}
const tr = (language, key) => ui[key]?.[language === 'rw' ? 1 : 0] || key

function Icon({ children }) { return <span className="icon" aria-hidden="true">{children}</span> }
function Logo({ onClick }) { return <button className="logo" onClick={onClick}><span className="logo-mark">g</span><span>gura<span>&</span>ugurisha</span></button> }
function Header({ page, go, cartCount, query, setQuery, language, setLanguage }) {
  const [menu, setMenu] = useState(false)
  return <header className="topbar"><div className="nav-wrap"><Logo onClick={() => go('home')} /><nav className={menu ? 'nav-links open' : 'nav-links'}>
    <button className={page === 'home' ? 'active' : ''} onClick={() => { go('home'); setMenu(false) }}>{tr(language, 'home')}</button>
    <button className={page === 'shop' ? 'active' : ''} onClick={() => { go('shop'); setMenu(false) }}>{tr(language, 'marketplace')}</button>
    <button className={page === 'repair' ? 'active' : ''} onClick={() => { go('repair'); setMenu(false) }}>{tr(language, 'repair')}</button>
    <button onClick={() => { go('shop'); setMenu(false) }}>{tr(language, 'categories')}</button>
    <button className={page === 'sell' ? 'active' : ''} onClick={() => { go('sell'); setMenu(false) }}>{tr(language, 'sell')}</button>
  </nav><div className="nav-actions"><label className="nav-search"><Icon>⌕</Icon><input value={query} onChange={(e) => setQuery(e.target.value)} onFocus={() => go('shop')} placeholder={tr(language, 'search')} aria-label={tr(language, 'search')} /></label><button className="language-toggle" onClick={() => setLanguage(language === 'en' ? 'rw' : 'en')} aria-label="Change language">{language === 'en' ? 'RW' : 'EN'}</button><button className="account-btn" onClick={() => go('login')}><Icon>◯</Icon><span>{tr(language, 'account')}</span></button><button className="cart-btn" onClick={() => go('cart')}><Icon>▱</Icon><span>{tr(language, 'cart')}</span>{cartCount > 0 && <b>{cartCount}</b>}</button></div><button className="menu-btn" onClick={() => setMenu(!menu)} aria-label="Open menu">☰</button></div></header>
}
function Footer({ go }) { return <footer><div className="footer-inner"><div><Logo onClick={() => go('home')} /><p className="footer-copy">A considered marketplace for buying and selling electronics in Rwanda.</p></div><div><h4>Marketplace</h4><button onClick={() => go('shop')}>Browse products</button><button onClick={() => go('sell')}>Sell a product</button><button onClick={() => go('repair')}>Repair portal</button></div><div><h4>Account</h4><button onClick={() => go('login')}>Log in</button><button onClick={() => go('register')}>Create account</button><button onClick={() => go('dashboard')}>Dashboard</button></div><div><h4>Contact</h4><p>[Contact information to be provided]</p><p className="muted">Demo marketplace for presentation</p></div></div><div className="footer-bottom"><span>© 2026 gura&ugurisha</span><span>Frontend prototype · Sample data</span></div></footer>}
function ProductCard({ product, add, open }) { return <article className="product-card"><button className="image-button" onClick={() => open(product)}><img src={product.image} alt={`${product.name} electronics product`} /><span className="condition">{product.condition}</span></button><div className="product-info"><div className="eyebrow">{product.category} · {product.location}</div><button className="product-name" onClick={() => open(product)}>{product.name}</button><p className="seller">Listed by {product.seller}</p><div className="card-bottom"><strong>{money(product.price)}</strong><button className="small-add" onClick={() => add(product)} aria-label={`Add ${product.name} to cart`}>+</button></div></div></article> }
function ProductGrid({ products, add, open }) { if (!products.length) return <div className="empty-state"><span className="empty-icon">⌕</span><h3>No products found</h3><p>Try a different search or remove one of the filters.</p></div>; return <div className="product-grid">{products.map((product) => <ProductCard key={product.id} product={product} add={add} open={open} />)}</div> }
function Home({ go, add, open, language }) { return <><main><section className="hero"><div className="hero-copy"><span className="kicker">{tr(language, 'heroKicker')}</span><h1>{tr(language, 'heroTitle')}<br /><em>{tr(language, 'heroTitle2')}</em></h1><p>{tr(language, 'heroCopy')}</p><div className="hero-actions"><button className="button primary" onClick={() => go('shop')}>{tr(language, 'shop')} <span>↗</span></button><button className="button text-button" onClick={() => go('sell')}>{tr(language, 'sellShort')} <span>↗</span></button></div><div className="proof"><span className="proof-avatars">●●●</span><span><b>{tr(language, 'made')}</b><br /><small>{tr(language, 'simple')}</small></span></div></div><div className="hero-art"><div className="art-tag">CURATED FINDS <span>✦</span></div><img src="https://images.unsplash.com/photo-1550009158-9ebf69173e03?auto=format&fit=crop&w=1200&q=85" alt="Modern electronics arranged on a desk" /><div className="art-note"><span>01</span><b>Find your next favorite device.</b></div></div></section><section className="section featured"><div className="section-heading"><div><span className="kicker">JUST IN</span><h2>{tr(language, 'fresh')}</h2></div><button className="underlined" onClick={() => go('shop')}>{tr(language, 'viewAll')} <span>↗</span></button></div><ProductGrid products={demoProducts.slice(0, 3)} add={add} open={open} /></section><section className="category-band"><div className="section-heading"><div><span className="kicker">SHOP BY TYPE</span><h2>{tr(language, 'shopType')}</h2></div></div><div className="category-row">{['Phones', 'Laptops', 'Audio', 'Gaming'].map((category, i) => <button key={category} onClick={() => go('shop', category)}><span className={`cat-icon cat-${i}`}>{['▣', '▤', '◒', '◈'][i]}</span><b>{category}</b><span>↗</span></button>)}</div></section><section className="trust"><div><span className="kicker">A BETTER WAY TO TRADE</span><h2>Technology should<br /><em>move you forward.</em></h2></div><div className="trust-items"><div><b>01 / Curated</b><p>Useful electronics, clearly presented and easy to compare.</p></div><div><b>02 / Local</b><p>Connect with buyers and sellers across Rwanda.</p></div><div><b>03 / Simple</b><p>A focused experience without the usual marketplace noise.</p></div></div></section><section className="sell-banner"><div><span className="kicker">HAVE TECH TO MOVE ON?</span><h2>Turn your unused<br />tech into opportunity.</h2></div><button className="button light" onClick={() => go('sell')}>{tr(language, 'sellShort')} <span>↗</span></button></section></main><Footer go={go} /></> }
function Filters({ category, setCategory, condition, setCondition, sort, setSort }) { return <div className="filters"><div className="filter-label">FILTERS <span>↓</span></div><select value={category} onChange={(e) => setCategory(e.target.value)} aria-label="Filter by category">{categories.map((item) => <option key={item}>{item}</option>)}</select><select value={condition} onChange={(e) => setCondition(e.target.value)} aria-label="Filter by condition"><option>Any condition</option><option>New</option><option>Like new</option><option>Good</option></select><select value={sort} onChange={(e) => setSort(e.target.value)} aria-label="Sort products"><option value="newest">Newest first</option><option value="low">Price: low to high</option><option value="high">Price: high to low</option></select></div> }
function Shop({ query, setQuery, add, open, initialCategory, language }) { const [category, setCategory] = useState(initialCategory || 'All'); const [condition, setCondition] = useState('Any condition'); const [sort, setSort] = useState('newest'); const products = useMemo(() => { let result = demoProducts.filter((p) => `${p.name} ${p.category} ${p.location}`.toLowerCase().includes(query.toLowerCase()) && (category === 'All' || p.category === category) && (condition === 'Any condition' || p.condition === condition)); if (sort === 'low') result.sort((a, b) => a.price - b.price); if (sort === 'high') result.sort((a, b) => b.price - a.price); return result }, [query, category, condition, sort]); return <main className="shop-page"><div className="page-intro"><span className="kicker">THE MARKETPLACE</span><h1>{language === 'rw' ? <>Shaka <br /><em>igikoresho ukunda.</em></> : <>Find your next<br /><em>favorite device.</em></>}</h1><p>{language === 'rw' ? 'Reba ibikoresho byiza bitangwa n’abacuruzi bo mu Rwanda.' : 'Browse quality electronics from sellers across Rwanda.'}</p></div><div className="shop-toolbar"><div className="wide-search"><Icon>⌕</Icon><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder={language === 'rw' ? 'Shakisha izina, icyiciro cyangwa aho giherereye' : 'Search by product, category or location'} aria-label="Search products" /></div><Filters {...{ category, setCategory, condition, setCondition, sort, setSort }} /></div><div className="results-line"><span>{products.length} {language === 'rw' ? 'ibicuruzwa' : 'products'}</span><span className="demo-note">Demo inventory · Sample data</span></div><ProductGrid products={products} add={add} open={open} /></main> }
function Detail({ product, add, go }) { return <main className="detail-page"><button className="back-link" onClick={() => go('shop')}>← Back to marketplace</button><div className="detail-layout"><div className="detail-image"><img src={product.image} alt={`${product.name} electronics product`} /><span>DEMO LISTING</span></div><div className="detail-copy"><span className="kicker">{product.category} · {product.location}</span><h1>{product.name}</h1><strong className="detail-price">{money(product.price)}</strong><p className="detail-description">{product.description}</p><div className="specs"><div><span>Condition</span><b>{product.condition}</b></div><div><span>Availability</span><b>Available</b></div><div><span>Seller</span><b>{product.seller}</b></div></div><button className="button primary full" onClick={() => add(product)}>Add to cart <span>＋</span></button><button className="button outline full" onClick={() => alert('Seller contact details will be connected when the marketplace backend is ready.')}>Contact seller <span>↗</span></button><p className="small-note">Contact actions are placeholders until seller contact details are provided.</p></div></div></main> }
function Cart({ cart, updateQty, remove, go }) { const total = cart.reduce((sum, item) => sum + item.price * item.qty, 0); return <main className="cart-page"><div className="page-intro compact"><span className="kicker">YOUR BAG</span><h1>Shopping cart</h1><p>{cart.length ? `${cart.reduce((s, i) => s + i.qty, 0)} items ready for checkout.` : 'Your saved products will appear here.'}</p></div>{!cart.length ? <div className="empty-state cart-empty"><span className="empty-icon">▱</span><h3>Your cart is empty</h3><p>Find something useful for your setup.</p><button className="button primary" onClick={() => go('shop')}>Browse products <span>↗</span></button></div> : <div className="cart-layout"><div className="cart-items">{cart.map((item) => <div className="cart-item" key={item.id}><img src={item.image} alt={item.name} /><div className="cart-item-copy"><span className="eyebrow">{item.category}</span><h3>{item.name}</h3><p>{money(item.price)}</p><div className="qty"><button onClick={() => updateQty(item.id, -1)}>-</button><span>{item.qty}</span><button onClick={() => updateQty(item.id, 1)}>+</button></div></div><button className="remove" onClick={() => remove(item.id)} aria-label={`Remove ${item.name}`}>×</button></div>)}</div><aside className="summary"><span className="kicker">ORDER SUMMARY</span><div><span>Subtotal</span><b>{money(total)}</b></div><div><span>Delivery</span><span className="muted">To be confirmed</span></div><hr /><div className="total"><span>Total</span><b>{money(total)}</b></div><button className="button primary full" onClick={() => go('checkout')}>Proceed to checkout <span>↗</span></button><button className="button outline full" onClick={() => go('shop')}>Continue shopping</button></aside></div>}</main> }
function Auth({ type, login, register, go }) { const [form, setForm] = useState({ name: '', email: '', phone: '', password: '', confirm: '' }); const [error, setError] = useState(''); const isLogin = type === 'login'; const submit = (e) => { e.preventDefault(); if (!form.email || !form.password || (!isLogin && (!form.name || form.password !== form.confirm))) return setError(isLogin ? 'Please enter your email and password.' : 'Complete all fields and make sure passwords match.'); setError(''); isLogin ? login(form) : register(form) }; return <main className="auth-page"><div className="auth-aside"><Logo onClick={() => go('home')} /><div><span className="kicker">THE SMARTER MARKETPLACE</span><h2>Tech that fits<br /><em>your life.</em></h2></div><span className="aside-foot">gura&ugurisha · Rwanda</span></div><div className="auth-form"><button className="back-link" onClick={() => go('home')}>← Back home</button><span className="kicker">{isLogin ? 'WELCOME BACK' : 'JOIN THE MARKETPLACE'}</span><h1>{isLogin ? 'Log in to your account' : 'Create your account'}</h1><p className="form-intro">{isLogin ? 'Your saved products and listings are waiting.' : 'Buy better. Sell smarter. It starts here.'}</p><form onSubmit={submit}>{!isLogin && <label>Full name<input value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} placeholder="Your full name" /></label>}<label>Email address<input type="email" value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} placeholder="you@example.com" /></label>{!isLogin && <label>Phone number<input value={form.phone} onChange={(e) => setForm({ ...form, phone: e.target.value })} placeholder="07X XXX XXX" /></label>}<label>Password<input type="password" value={form.password} onChange={(e) => setForm({ ...form, password: e.target.value })} placeholder="Enter your password" /></label>{!isLogin && <label>Confirm password<input type="password" value={form.confirm} onChange={(e) => setForm({ ...form, confirm: e.target.value })} placeholder="Repeat your password" /></label>}{error && <p className="form-error">{error}</p>}<button className="button primary full" type="submit">{isLogin ? 'Log in' : 'Create account'} <span>↗</span></button></form><p className="switch-auth">{isLogin ? 'New to gura&ugurisha?' : 'Already have an account?'} <button onClick={() => go(isLogin ? 'register' : 'login')}>{isLogin ? 'Create an account' : 'Log in'}</button></p><p className="small-note">Demo authentication only. A real account system requires a secure backend.</p></div></main> }
function Sell({ addListing, go }) { const [form, setForm] = useState({ name: '', category: 'Phones', price: '', condition: 'New', location: 'Kigali', description: '', quantity: '1' }); const [sent, setSent] = useState(false); const submit = (e) => { e.preventDefault(); if (!form.name || !form.price || !form.description) return; addListing({ ...form, price: Number(form.price), id: Date.now(), image: 'https://images.unsplash.com/photo-1550745165-9bc0b252726f?auto=format&fit=crop&w=900&q=80', seller: 'Your demo profile' }); setSent(true) }; if (sent) return <main className="success-page"><span className="success-mark">✓</span><span className="kicker">LISTING SAVED</span><h1>Your product is<br /><em>ready to be seen.</em></h1><p>This frontend demo saved your listing locally. It is not published to a live marketplace yet.</p><div><button className="button primary" onClick={() => go('dashboard')}>View dashboard <span>↗</span></button><button className="button text-button" onClick={() => setSent(false)}>Add another</button></div></main>; return <main className="form-page"><div className="page-intro compact"><span className="kicker">SELL WITH US</span><h1>List a product</h1><p>Give a great device a second life with a clear, honest listing.</p></div><form className="listing-form" onSubmit={submit}><div className="form-section"><span className="step">01</span><div><h2>Product details</h2><p>Tell buyers what you are offering.</p></div></div><label>Product name *<input value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} placeholder="e.g. iPhone 13 Pro" required /></label><div className="form-row"><label>Category *<select value={form.category} onChange={(e) => setForm({ ...form, category: e.target.value })}>{categories.slice(1).map((c) => <option key={c}>{c}</option>)}</select></label><label>Price (RWF) *<input type="number" value={form.price} onChange={(e) => setForm({ ...form, price: e.target.value })} placeholder="250000" required min="1" /></label></div><div className="form-row"><label>Condition<select value={form.condition} onChange={(e) => setForm({ ...form, condition: e.target.value })}><option>New</option><option>Like new</option><option>Good</option></select></label><label>Location<select value={form.location} onChange={(e) => setForm({ ...form, location: e.target.value })}><option>Kigali</option><option>Huye</option><option>Musanze</option><option>Other</option></select></label></div><label>Description *<textarea value={form.description} onChange={(e) => setForm({ ...form, description: e.target.value })} placeholder="Describe the product's condition, features and what is included." rows="5" required /></label><div className="image-upload"><span>＋</span><div><b>Product images</b><p>Image upload will connect to storage in the production version.</p></div></div><div className="form-actions"><button type="button" className="button outline" onClick={() => go('home')}>Cancel</button><button className="button primary" type="submit">Publish demo listing <span>↗</span></button></div></form></main> }
function Checkout({ cart, go }) { const total = cart.reduce((sum, item) => sum + item.price * item.qty, 0); const [placed, setPlaced] = useState(false); if (placed) return <main className="success-page"><span className="success-mark">✓</span><span className="kicker">ORDER RECEIVED</span><h1>Thanks for<br /><em>shopping local.</em></h1><p>Your demo order has been recorded in this browser. No payment was processed.</p><button className="button primary" onClick={() => go('home')}>Back to home <span>↗</span></button></main>; return <main className="checkout-page"><div className="page-intro compact"><span className="kicker">CHECKOUT</span><h1>Almost yours.</h1><p>Complete the details below to review your demo order.</p></div><div className="checkout-layout"><form className="checkout-form" onSubmit={(e) => { e.preventDefault(); setPlaced(true) }}><h2>Customer information</h2><div className="form-row"><label>Full name<input required placeholder="Your full name" /></label><label>Email address<input type="email" required placeholder="you@example.com" /></label></div><label>Phone number<input required placeholder="07X XXX XXX" /></label><label>Delivery / location<input required placeholder="City or neighborhood" /></label><div className="payment-placeholder"><span className="card-symbol">▣</span><div><b>Card payment integration</b><p>Connect a secure payment provider here. This prototype does not collect or store card details.</p></div><span className="status-pill">NOT CONNECTED</span></div><button className="button primary full" type="submit">Place demo order <span>↗</span></button></form><aside className="summary"><span className="kicker">ORDER SUMMARY</span>{cart.map((item) => <div className="summary-item" key={item.id}><span>{item.name} <b>× {item.qty}</b></span><strong>{money(item.price * item.qty)}</strong></div>)}<hr /><div className="total"><span>Total</span><b>{money(total)}</b></div></aside></div></main> }
function Repair({ go }) { const [form, setForm] = useState({ device: 'Phone', brand: '', issue: '', location: 'Kigali', pickup: 'Drop off' }); const [sent, setSent] = useState(false); const submit = (e) => { e.preventDefault(); if (form.brand && form.issue) setSent(true) }; if (sent) return <main className="repair-success success-page"><span className="success-mark">✓</span><span className="kicker">REPAIR REQUEST SAVED</span><h1>We have your<br /><em>device details.</em></h1><p>This demo request is stored only for this session. A real repair partner and booking time will be connected later.</p><button className="button primary" onClick={() => go('home')}>Back to home <span>↗</span></button></main>; return <main className="repair-page"><section className="repair-hero"><div><span className="kicker">THE REPAIR DESK</span><h1>Keep your tech<br /><em>in the game.</em></h1><p>Tell us what is wrong with your device and prepare a repair request in under a minute.</p><div className="repair-stats"><span><b>01</b> Diagnose the issue</span><span><b>02</b> Choose a handover</span><span><b>03</b> Get a follow-up</span></div></div><div className="repair-visual"><span>DEVICE CARE / 2026</span><div className="repair-device">⌁</div><b>From cracked screens<br />to tired batteries.</b></div></section><section className="repair-body"><div className="repair-intro"><span className="kicker">START A REQUEST</span><h2>What needs attention?</h2><p>Repair pricing and appointment details are not connected yet, so no payment is requested here.</p></div><form className="repair-form" onSubmit={submit}><div className="form-row"><label>Device type<select value={form.device} onChange={(e) => setForm({ ...form, device: e.target.value })}><option>Phone</option><option>Laptop</option><option>Tablet</option><option>TV</option><option>Camera</option><option>Game console</option><option>Audio device</option></select></label><label>Brand / model *<input value={form.brand} onChange={(e) => setForm({ ...form, brand: e.target.value })} placeholder="e.g. iPhone 13" required /></label></div><label>What is happening? *<textarea value={form.issue} onChange={(e) => setForm({ ...form, issue: e.target.value })} placeholder="Describe the problem, symptoms or damage." rows="5" required /></label><div className="repair-options"><span className="option-title">Preferred handover</span><label className="radio-option"><input type="radio" name="pickup" checked={form.pickup === 'Drop off'} onChange={() => setForm({ ...form, pickup: 'Drop off' })} /> Drop off at a partner desk</label><label className="radio-option"><input type="radio" name="pickup" checked={form.pickup === 'Collection'} onChange={() => setForm({ ...form, pickup: 'Collection' })} /> Request collection details</label></div><label>Preferred area<select value={form.location} onChange={(e) => setForm({ ...form, location: e.target.value })}><option>Kigali</option><option>Huye</option><option>Musanze</option><option>Rubavu</option></select></label><button className="button primary" type="submit">Send repair request <span>↗</span></button></form></section></main> }
function Dashboard({ user, listings, go, logout }) { return <main className="dashboard"><div className="dash-head"><div><span className="kicker">YOUR SPACE</span><h1>Good to see you, {user?.name?.split(' ')[0] || 'there'}.</h1><p>Manage your marketplace activity in one place.</p></div><button className="button outline" onClick={logout}>Log out</button></div><div className="dashboard-grid"><section><div className="dash-title"><h2>My listings</h2><button onClick={() => go('sell')}>+ Add product</button></div>{listings.length ? listings.map((item) => <div className="listing-row" key={item.id}><img src={item.image} alt={item.name} /><div><b>{item.name}</b><span>{money(item.price)} · {item.location}</span></div><span className="status-pill live">DEMO</span></div>) : <div className="dash-empty"><p>You have not listed anything yet.</p><button onClick={() => go('sell')}>Create your first listing ↗</button></div>}</section><section className="activity"><h2>Quick links</h2><button onClick={() => go('cart')}><span>▱</span><div><b>Your cart</b><small>Review saved products</small></div>↗</button><button onClick={() => go('shop')}><span>⌕</span><div><b>Browse marketplace</b><small>Find something new</small></div>↗</button><div className="demo-callout"><span>i</span><p>This is a frontend demo. Profile and order data will connect to a backend later.</p></div></section></div></main> }
export default function App() { const [page, setPage] = useState('home'); const [query, setQuery] = useState(''); const [selected, setSelected] = useState(null); const [language, setLanguage] = useState(() => localStorage.getItem('gura-language') || 'en'); const [user, setUser] = useState(() => JSON.parse(localStorage.getItem('gura-user') || 'null')); const [cart, setCart] = useState(() => JSON.parse(localStorage.getItem('gura-cart') || '[]')); const [listings, setListings] = useState(() => JSON.parse(localStorage.getItem('gura-listings') || '[]')); useEffect(() => { localStorage.setItem('gura-language', language); document.documentElement.lang = language === 'rw' ? 'rw' : 'en' }, [language]); useEffect(() => localStorage.setItem('gura-cart', JSON.stringify(cart)), [cart]); useEffect(() => localStorage.setItem('gura-listings', JSON.stringify(listings)), [listings]); useEffect(() => { if (user) localStorage.setItem('gura-user', JSON.stringify(user)); else localStorage.removeItem('gura-user') }, [user]); const go = (next, category) => { setPage(next); if (category) setQuery(category); window.scrollTo({ top: 0, behavior: 'smooth' }) }; const add = (product) => { setCart((current) => { const existing = current.find((item) => item.id === product.id); return existing ? current.map((item) => item.id === product.id ? { ...item, qty: item.qty + 1 } : item) : [...current, { ...product, qty: 1 }] }); }; const updateQty = (id, change) => setCart((current) => current.map((item) => item.id === id ? { ...item, qty: Math.max(1, item.qty + change) } : item)); const remove = (id) => setCart((current) => current.filter((item) => item.id !== id)); const login = (details) => { setUser({ name: details.email.split('@')[0], email: details.email }); go('dashboard') }; const register = (details) => { setUser({ name: details.name, email: details.email }); go('dashboard') }; const logout = () => { setUser(null); go('home') }; let content = <Home go={go} add={add} language={language} open={(p) => { setSelected(p); go('detail') }} />; if (page === 'shop') content = <Shop query={query} setQuery={setQuery} language={language} add={add} open={(p) => { setSelected(p); go('detail') }} />; if (page === 'detail' && selected) content = <Detail product={selected} add={add} go={go} />; if (page === 'cart') content = <Cart cart={cart} updateQty={updateQty} remove={remove} go={go} />; if (page === 'checkout') content = <Checkout cart={cart} go={go} />; if (page === 'repair') content = <Repair go={go} />; if (page === 'login' || page === 'register') content = <Auth type={page} login={login} register={register} go={go} />; if (page === 'sell') content = <Sell addListing={(listing) => setListings([...listings, listing])} go={go} />; if (page === 'dashboard') content = user ? <Dashboard user={user} listings={listings} go={go} logout={logout} /> : <Auth type="login" login={login} register={register} go={go} />; const standalone = page === 'login' || page === 'register'; return <div className={standalone ? 'app standalone' : 'app'}>{!standalone && <Header page={page} go={go} cartCount={cart.reduce((sum, item) => sum + item.qty, 0)} query={query} setQuery={setQuery} language={language} setLanguage={setLanguage} />}{content}</div> }
